# Contributing

Every ported public function must update the parity ledger status, include tests, preserve governance/failure semantics, and document any intentional R/Python divergence. No optional Bayesian backend may become a mandatory dependency of the core contract layer.
